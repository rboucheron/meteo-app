export interface City {
    id: number;
    name: string; 
    coord: Array<number>;
    country: string; 
    population: number; 
    timezone: number;
    sunrise: number; 
    sunset: number; 
  }
